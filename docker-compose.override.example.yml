services:
  taxi-web:
    build:
      context: .
    image: registry.dev.it59com.ru/taxi-platform/taxi-web:latest
    ports:
      - "5173:80"
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://backend.host/api/v1}
      - VITE_WS_URL=${VITE_WS_URL:-ws://backend.host/ws}